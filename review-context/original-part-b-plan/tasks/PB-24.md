# PB-24 — Prepare performance-blind data and research-scope handoff without building models

**Legacy mapping:** LV-07
**Dependencies:** PB-23
**External input gate:** NONE
**Status:** PLANNED — no implementation claim.

## Files owned/modified
- `src/moj_discovery/data_manifest.py`
- `tests/live/test_data_manifest.py`
- `docs/live/research-scope.md`

## Interfaces to produce
- `data_manifest.py::freeze_dataset_manifest(run_refs: Sequence[RunRef], quality_view: QualityView) -> DataManifest`

## Read before implementation
R4; A1 in `SOURCES.md`; the relevant specification in02..06. File paths are repo-relative, not paths in this plan package. Future paths are created by their owner before verification, not required at PB-00.

## Test-first execution
- [ ] Write focused positive/rejection tests in the exact test files below. A first red must fail because the intended behavior is wrong/missing, not an unrelated import/configuration error.
- [ ] Expose only identity, coverage, timing, corrections, missingness, quota, cost and data-rights fields to scope selection. Retain actual targets in research inputs without exposing model performance.
- [ ] Reference observed sources and license/access evidence. Mark missing historical data and propose chronological partitions without training a model.
- [ ] Keep H1/H2/FT settlement semantics explicit and preserve existing SCOPE0 approval requirements. Do not choose a competition after seeing model ROI.
- [ ] Hand off actual availability and unknowns to Part C, then stop; do not start RS-01 automatically.
- [ ] Run all owned tests and affected regression checks. Inspect actual outputs and diff; preserve failed evidence. Fix code rather than change expected values to get green.
- [ ] Checkpoint task ID, code/config hashes, command exits and actual evidence in `.local/part-b/task-evidence/PB-24.json`; update `docs/execution/PART_B_PROGRESS.md`. Commit only task files under explicit local-commit authority; never push.

## Behavioral assertions to implement in owned tests
These are specification assertions; helper fixtures belong to this task’s test files. They are not an assertion that functions or a live data set already exist.

```text
assert "roi" not in quality_view
assert "model_accuracy" not in quality_view
assert dataset.has_source_provenance is True
```

## Exact verification commands
Commands run from repo root after owning tasks exist. Commands needing real data or user secret run only at the external gate, in the user terminal.

```bash
uv run --frozen --offline python -m pytest -q tests/live/test_data_manifest.py
```

## Acceptance
- A dataset/quality manifest is ready for research-scope review. No forecast, profit claim or Part C implementation is produced. The Part B batch ends.

## Error handling and continuation
Ordinary test/code errors are fixed automatically; then continue the next ready task. A missing key/profile/approval/platform is WAITING_INPUT with one precise user instruction, not a request after every task. Run other ready independent tasks meanwhile. Never treat synthetic data as a substitute for a real-case gate. Authentication failure, scope mismatch, secret leakage or an unsafe privilege change is a stop for the affected real source. Quota/runtime limits never auto-expand.

## Rollback
Stop owned source/session if needed, preserve evidence, and revert only this task’s change through a normal reviewed commit. No hard reset, force push, deletion of other work or mutation of old PASS receipts.
