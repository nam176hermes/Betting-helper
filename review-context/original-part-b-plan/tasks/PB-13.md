# PB-13 — Build Side Panel and multi-match workspace without model placeholders

**Legacy mapping:** LV-05
**Dependencies:** PB-12
**External input gate:** NONE
**Status:** PLANNED — no implementation claim.

## Files owned/modified
- `extension/src/live/panel.ts`
- `extension/src/live/panel.html`
- `extension/src/live/panel.css`
- `extension/src/live/workspace.ts`
- `extension/src/live/workspace.html`
- `src/moj_discovery/workspace_projection.py`
- `extension/test/live/panel.test.ts`
- `tests/live/test_workspace_projection.py`

## Interfaces to produce
- `extension/src/live/panel.ts#renderMatch`
- `extension/src/live/workspace.ts#renderWatchlist`
- `workspace_projection.py::project_watchlist(state: WorkspaceState) -> WorkspaceView`

## Read before implementation
C1; C2; C4 in `SOURCES.md`; the relevant specification in02..06. File paths are repo-relative, not paths in this plan package. Future paths are created by their owner before verification, not required at PB-00.

## Test-first execution
- [ ] Write focused positive/rejection tests in the exact test files below. A first red must fail because the intended behavior is wrong/missing, not an unrelated import/configuration error.
- [ ] Render the fields and controls in05_OPERATOR_AND_UI.md. Show source clock precision, observed odds and MODEL_NOT_QUALIFIED. Do not manufacture probabilities or recommendations.
- [ ] Open the panel on a user gesture; use exact verified operator/LiveScore links. Shared watchlist and evidence are read-only except enumerated user commands. Escape all text and allow HTTPS links only.
- [ ] Panel closure changes subscriptions, not collection ownership. Refresh requests are coalesced by backend. The accepted stage controls1/3/5 matches; an interface constant cannot enlarge scope.
- [ ] Test narrow320px layout, keyboard navigation, malicious team text, STALE/NO_DATA states and switching between horizons/matches without showing a previous fixture’s prices.
- [ ] Run all owned tests and affected regression checks. Inspect actual outputs and diff; preserve failed evidence. Fix code rather than change expected values to get green.
- [ ] Checkpoint task ID, code/config hashes, command exits and actual evidence in `.local/part-b/task-evidence/PB-13.json`; update `docs/execution/PART_B_PROGRESS.md`. Commit only task files under explicit local-commit authority; never push.

## Behavioral assertions to implement in owned tests
These are specification assertions; helper fixtures belong to this task’s test files. They are not an assertion that functions or a live data set already exist.

```text
assert panel.text.includes("MODEL_NOT_QUALIFIED")
assert panel.getByRole("button", {name: "Bet"}).count() == 0
assert workspace.activeSnapshot.revision == panel.snapshot.revision
```

## Exact verification commands
Commands run from repo root after owning tasks exist. Commands needing real data or user secret run only at the external gate, in the user terminal.

```bash
pnpm --dir extension exec tsc -p tsconfig.test.json
node --test extension/.test-build/test/live/panel.test.js
uv run --frozen --offline python -m pytest -q tests/live/test_workspace_projection.py
```

## Acceptance
- A working mock panel is clearly labelled as synthetic. Panel and watchlist share the same projection revision. Neither contains betting or Cashout controls.

## Error handling and continuation
Ordinary test/code errors are fixed automatically; then continue the next ready task. A missing key/profile/approval/platform is WAITING_INPUT with one precise user instruction, not a request after every task. Run other ready independent tasks meanwhile. Never treat synthetic data as a substitute for a real-case gate. Authentication failure, scope mismatch, secret leakage or an unsafe privilege change is a stop for the affected real source. Quota/runtime limits never auto-expand.

## Rollback
Stop owned source/session if needed, preserve evidence, and revert only this task’s change through a normal reviewed commit. No hard reset, force push, deletion of other work or mutation of old PASS receipts.
