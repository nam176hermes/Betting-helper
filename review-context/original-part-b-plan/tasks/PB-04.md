# PB-04 — Implement the fixed direct API client with bounded GET and safe response handling

**Legacy mapping:** LV-02
**Dependencies:** PB-02, PB-03
**External input gate:** NONE
**Status:** PLANNED — no implementation claim.

## Files owned/modified
- `src/moj_discovery/providers/api_football.py`
- `src/moj_discovery/provider_protocol.py`
- `tests/live/test_api_football.py`
- `tests/live/test_provider_http_boundary.py`

## Interfaces to produce
- `api_football.py::ApiFootballClient.get_status() -> SafeStatus`
- `ApiFootballClient.get_coverage(league_id: int, season: int) -> Coverage`
- `ApiFootballClient.get_fixture_bundle(ids: Sequence[int]) -> ProviderResponse`
- `ApiFootballClient.get_events(fixture_id: int) -> ProviderResponse`
- `ApiFootballClient.get_fixtures_for_date(league_id: int, season: int, date: str) -> ProviderResponse`

## Read before implementation
A1; A3; A6; P2; P6 in `SOURCES.md`; the relevant specification in02..06. File paths are repo-relative, not paths in this plan package. Future paths are created by their owner before verification, not required at PB-00.

## Test-first execution
- [ ] Write focused positive/rejection tests in the exact test files below. A first red must fail because the intended behavior is wrong/missing, not an unrelated import/configuration error.
- [ ] Build endpoint paths only from fixed purpose enums and validated parameters; ids sorted unique<=20, direct x-apisports-key header. No RapidAPI, arbitrary URL, redirect, ambient proxy or disabledTLS.
- [ ] Reserve call budget before HTTP. Use10s timeout8MiB response bound, no raw HTTP dump; errors withHTTP200 handled before response. Parse status response privately and project quota fields only.
- [ ] Mock HTTP returns missing/wrong IDs, partial response, wrong schema, paging>1,401/403,429 Retry-After date/delta,5xx/499/timeout, oversize and malicious redirect. Every retry is admitted by shared quota.
- [ ] Implement existing provider_protocol entrypoints only for typed live-v2 probe scope; legacy stubs/receipts are not bypassed. Real requests disabled untilPB-18/user intent.
- [ ] Run all owned tests and affected regression checks. Inspect actual outputs and diff; preserve failed evidence. Fix code rather than change expected values to get green.
- [ ] Checkpoint task ID, code/config hashes, command exits and actual evidence in `.local/part-b/task-evidence/PB-04.json`; update `docs/execution/PART_B_PROGRESS.md`. Commit only task files under explicit local-commit authority; never push.

## Behavioral assertions to implement in owned tests
These are specification assertions; helper fixtures belong to this task’s test files. They are not an assertion that functions or a live data set already exist.

```text
client.get_fixture_bundle([103, 101, 103])
assert mock_http.last_query == "ids=101-103"
assert "api_key" not in safe_report
assert redirect_target.requests == []
```

## Exact verification commands
Commands run from repo root after owning tasks exist. Commands needing real data or user secret run only at the external gate, in the user terminal.

```bash
uv run --frozen --offline python -m pytest -q tests/live/test_api_football.py tests/live/test_provider_http_boundary.py
```

## Acceptance
- Query/header construction is correct. Authentication failure stops without a retry storm. No secret leaves the fixed API authority or enters evidence. Mock tests perform zero external network calls.

## Error handling and continuation
Ordinary test/code errors are fixed automatically; then continue the next ready task. A missing key/profile/approval/platform is WAITING_INPUT with one precise user instruction, not a request after every task. Run other ready independent tasks meanwhile. Never treat synthetic data as a substitute for a real-case gate. Authentication failure, scope mismatch, secret leakage or an unsafe privilege change is a stop for the affected real source. Quota/runtime limits never auto-expand.

## Rollback
Stop owned source/session if needed, preserve evidence, and revert only this task’s change through a normal reviewed commit. No hard reset, force push, deletion of other work or mutation of old PASS receipts.
