# PB-07 — Implement typed multi-fixture LiveStore and independent replay

**Legacy mapping:** LV-03/04
**Dependencies:** PB-01, PB-05
**External input gate:** NONE
**Status:** PLANNED — no implementation claim.

## Files owned/modified
- `contracts/live_readonly/v1/live-store.sql`
- `src/moj_discovery/live_store.py`
- `src/moj_discovery/live_replay.py`
- `tests/live/test_live_store.py`
- `tests/live/test_live_replay.py`

## Interfaces to produce
- `live_store.py::LiveStore.append(event: LiveEvent) -> DurableReceipt`
- `live_store.py::LiveStore.read_projection(binding_id: str) -> MatchView`
- `live_replay.py::verify_live_replay(run_dir: Path, target_dir: Path) -> ReplayResult`

## Read before implementation
R4; P3; P5 in `SOURCES.md`; the relevant specification in02..06. File paths are repo-relative, not paths in this plan package. Future paths are created by their owner before verification, not required at PB-00.

## Test-first execution
- [ ] Write focused positive/rejection tests in the exact test files below. A first red must fail because the intended behavior is wrong/missing, not an unrelated import/configuration error.
- [ ] Create new live DB from supplied DDL; preserve existing RunStore vendor. Register run and binding before first event; no imported fake approved rows.
- [ ] Implement canonical event chain, contiguous source cursor, actual payload+projection commit, postcommit receipt, duplicate samebytes and conflict/gap controls.
- [ ] Replay frozen actual live payloads/control timestamps into new DB using same reducer; compare independent readback, never copy derived rows.
- [ ] Fault tests kill owned DB writer before/aftercommit, remove/tamper raw record, reorder receive index, corrupt quote/binding and replay twice. Record actual effects and keep expected parent-only.
- [ ] Run all owned tests and affected regression checks. Inspect actual outputs and diff; preserve failed evidence. Fix code rather than change expected values to get green.
- [ ] Checkpoint task ID, code/config hashes, command exits and actual evidence in `.local/part-b/task-evidence/PB-07.json`; update `docs/execution/PART_B_PROGRESS.md`. Commit only task files under explicit local-commit authority; never push.

## Behavioral assertions to implement in owned tests
These are specification assertions; helper fixtures belong to this task’s test files. They are not an assertion that functions or a live data set already exist.

```text
receipt = store.append(valid_event)
assert store.append(valid_event) == receipt
assert verify_live_replay(run_dir, fresh_dir).equal
with pytest.raises(ValueError):
    store.append(conflicting_same_position)
```

## Exact verification commands
Commands run from repo root after owning tasks exist. Commands needing real data or user secret run only at the external gate, in the user terminal.

```bash
uv run --frozen --offline python -m pytest -q tests/live/test_live_store.py tests/live/test_live_replay.py
```

## Acceptance
- The real live store and a fresh replay database produce the same logical state. Partial failure never creates a false ACK. Independent expected/actual mutations are detected. Live records never enter the synthetic RawObservation contract.

## Error handling and continuation
Ordinary test/code errors are fixed automatically; then continue the next ready task. A missing key/profile/approval/platform is WAITING_INPUT with one precise user instruction, not a request after every task. Run other ready independent tasks meanwhile. Never treat synthetic data as a substitute for a real-case gate. Authentication failure, scope mismatch, secret leakage or an unsafe privilege change is a stop for the affected real source. Quota/runtime limits never auto-expand.

## Rollback
Stop owned source/session if needed, preserve evidence, and revert only this task’s change through a normal reviewed commit. No hard reset, force push, deletion of other work or mutation of old PASS receipts.
