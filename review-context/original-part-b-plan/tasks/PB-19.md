# PB-19 — Observe one dedicated operator tab and admit a real sanitized extraction profile

**Legacy mapping:** LV-01
**Dependencies:** PB-17
**External input gate:** USER_OPERATOR_SESSION_AND_PASSIVE_OBSERVATION_APPROVAL
**Status:** PLANNED — no implementation claim.

## Files owned/modified
- `docs/live/operator-capability-report.md`
- `tests/live/fixtures/admitted-profile-index.json`
- `tests/live/test_operator_profile_samples.py`

## Interfaces to produce
- `AdmittedProfileEvidence: exact observed routes; field map; participant-orientation checks; sanitized sample hashes; profile hash; current review reference.`

## Read before implementation
C3; C4; R3 in `SOURCES.md`; the relevant specification in02..06. File paths are repo-relative, not paths in this plan package. Future paths are created by their owner before verification, not required at PB-00.

## Test-first execution
- [ ] Write focused positive/rejection tests in the exact test files below. A first red must fail because the intended behavior is wrong/missing, not an unrelated import/configuration error.
- [ ] Ask the user to open one selected match in the dedicated Chrome profile and log in manually. Confirm the10minute read-only observation scope and actual platform. No account secrets are requested.
- [ ] Use the reviewed fixed DOM reader to inspect only selected fixture labels, IDs, three odds and market context. Do not read full HTML, account/balance areas, cookies or click a selection.
- [ ] Populate the extraction profile from observed sanitized samples. Verify HOME/AWAY independent of page order, horizon labels and normal-time settlement. Inaccessible data remains unsupported, not guessed.
- [ ] Add admitted nonsecret samples and regression tests. Any profile-specific parser change requires affected security tests and independent review before enabling live capture.
- [ ] Run all owned tests and affected regression checks. Inspect actual outputs and diff; preserve failed evidence. Fix code rather than change expected values to get green.
- [ ] Checkpoint task ID, code/config hashes, command exits and actual evidence in `.local/part-b/task-evidence/PB-19.json`; update `docs/execution/PART_B_PROGRESS.md`. Commit only task files under explicit local-commit authority; never push.

## Behavioral assertions to implement in owned tests
These are specification assertions; helper fixtures belong to this task’s test files. They are not an assertion that functions or a live data set already exist.

```text
assert profile.source_kind == "OBSERVED_REAL"
assert profile.orientation_status == "VERIFIED"
assert set(profile.horizons) >= {"FT"}
```

## Exact verification commands
Commands run from repo root after owning tasks exist. Commands needing real data or user secret run only at the external gate, in the user terminal.

```bash
uv run --frozen --offline python -m pytest -q tests/live/test_operator_profile_samples.py
```

## Acceptance
- A real one-fixture FT extraction profile is accepted, or a specific unsupported result is recorded. No guessed selectors or account data enter the profile.

## Error handling and continuation
Ordinary test/code errors are fixed automatically; then continue the next ready task. A missing key/profile/approval/platform is WAITING_INPUT with one precise user instruction, not a request after every task. Run other ready independent tasks meanwhile. Never treat synthetic data as a substitute for a real-case gate. Authentication failure, scope mismatch, secret leakage or an unsafe privilege change is a stop for the affected real source. Quota/runtime limits never auto-expand.

## Rollback
Stop owned source/session if needed, preserve evidence, and revert only this task’s change through a normal reviewed commit. No hard reset, force push, deletion of other work or mutation of old PASS receipts.
