# PB-15 — Qualify the actual browser-to-backend platform using synthetic local data

**Legacy mapping:** LV-04/06
**Dependencies:** PB-08, PB-10, PB-13, PB-14
**External input gate:** USER_PLATFORM_SELECTION
**Status:** PLANNED — no implementation claim.

## Files owned/modified
- `tools/qualify_live_platform.py`
- `tests/live/test_platform_binding.py`
- `tests/live/test_local_bridge.py`
- `docs/live/platform-qualification.md`

## Interfaces to produce
- `tools/qualify_live_platform.py::qualify_platform(config: LiveConfig, output: Path) -> PlatformResult`

## Read before implementation
W1; C2; C3; P4 in `SOURCES.md`; the relevant specification in02..06. File paths are repo-relative, not paths in this plan package. Future paths are created by their owner before verification, not required at PB-00.

## Test-first execution
- [ ] Write focused positive/rejection tests in the exact test files below. A first red must fail because the intended behavior is wrong/missing, not an unrelated import/configuration error.
- [ ] Record the actual Windows Chrome→WSL topology or the explicitly chosen Linux alternative. Test browser identity, local Origin/path and listener scope with synthetic data only.
- [ ] Do not open operator pages or request the API key. Never change a firewall, networking mode or bind0.0.0.0 automatically if localhost forwarding fails.
- [ ] Exercise panel close, worker termination and reconnect in owned test profiles. Sleep/wake and process kill require user approval if they affect the actual platform. Record executed versus unobserved cases.
- [ ] Linux evidence does not qualify Windows. A missing browser/display produces WAITING_PLATFORM; independent code tasks continue, live activation remains blocked.
- [ ] Run all owned tests and affected regression checks. Inspect actual outputs and diff; preserve failed evidence. Fix code rather than change expected values to get green.
- [ ] Checkpoint task ID, code/config hashes, command exits and actual evidence in `.local/part-b/task-evidence/PB-15.json`; update `docs/execution/PART_B_PROGRESS.md`. Commit only task files under explicit local-commit authority; never push.

## Behavioral assertions to implement in owned tests
These are specification assertions; helper fixtures belong to this task’s test files. They are not an assertion that functions or a live data set already exist.

```text
assert platform_result.browser_sha256 == hash(actual_binary)
assert server.bound_addresses == ["127.0.0.1"]
assert platform_result.operator_observed is False
```

## Exact verification commands
Commands run from repo root after owning tasks exist. Commands needing real data or user secret run only at the external gate, in the user terminal.

```bash
uv run --frozen --offline python -m pytest -q tests/live/test_platform_binding.py tests/live/test_local_bridge.py
```

## Acceptance
- The selected platform is tested on a real local path or explicitly marked pending. No unrelated user browser is terminated. A real API key is not needed.

## Error handling and continuation
Ordinary test/code errors are fixed automatically; then continue the next ready task. A missing key/profile/approval/platform is WAITING_INPUT with one precise user instruction, not a request after every task. Run other ready independent tasks meanwhile. Never treat synthetic data as a substitute for a real-case gate. Authentication failure, scope mismatch, secret leakage or an unsafe privilege change is a stop for the affected real source. Quota/runtime limits never auto-expand.

## Rollback
Stop owned source/session if needed, preserve evidence, and revert only this task’s change through a normal reviewed commit. No hard reset, force push, deletion of other work or mutation of old PASS receipts.
