# PB-06 — Implement one adaptive ProviderBundlePoller for the complete watchlist

**Legacy mapping:** LV-02/04
**Dependencies:** PB-03, PB-04, PB-05
**External input gate:** NONE
**Status:** PLANNED — no implementation claim.

## Files owned/modified
- `src/moj_discovery/providers/bundle_poller.py`
- `tests/live/test_bundle_poller.py`
- `tests/live/test_batch_request_counts.py`

## Interfaces to produce
- `bundle_poller.py::ProviderBundlePoller.set_watchlist(ids: Sequence[int], scope_revision: int) -> None`
- `ProviderBundlePoller.tick(now_mono: float) -> PollOutcome`
- `ProviderBundlePoller.stop(reason: str) -> None`

## Read before implementation
A1; A2; A5 in `SOURCES.md`; the relevant specification in02..06. File paths are repo-relative, not paths in this plan package. Future paths are created by their owner before verification, not required at PB-00.

## Test-first execution
- [ ] Write focused positive/rejection tests in the exact test files below. A first red must fail because the intended behavior is wrong/missing, not an unrelated import/configuration error.
- [ ] Apply exact status/deadline table from02_PROVIDER, fastest-active union for<=5. One shared in-flight request; no requests on UI re-render/panel reopen.
- [ ] Full watcher IDs included at each due tick, numeric sort/dedupe; status/coverage/terminal controls recorded. Observe terminal once then+30/+120 checks bounded by run deadline.
- [ ] No catch-up after missed ticks; rejected/budget exhausted/unknownquota =>visiblePAUSED/STALE. Manual refresh coalesces. Fallback events defaultOFF and cannot activate from empty array.
- [ ] Use fake clock and mock transport to count requests for single/five simultaneous, staggered windows, HT/live overlap, slow HTTP,429, sleep/resume and fallback insufficiency.
- [ ] Run all owned tests and affected regression checks. Inspect actual outputs and diff; preserve failed evidence. Fix code rather than change expected values to get green.
- [ ] Checkpoint task ID, code/config hashes, command exits and actual evidence in `.local/part-b/task-evidence/PB-06.json`; update `docs/execution/PART_B_PROGRESS.md`. Commit only task files under explicit local-commit authority; never push.

## Behavioral assertions to implement in owned tests
These are specification assertions; helper fixtures belong to this task’s test files. They are not an assertion that functions or a live data set already exist.

```text
for second in range(7200):
    poller.tick(float(second))
assert http.bundle_calls == 480  # all five kept1H/2H in this synthetic schedule
assert http.maximum_in_flight == 1
```

## Exact verification commands
Commands run from repo root after owning tasks exist. Commands needing real data or user secret run only at the external gate, in the user terminal.

```bash
uv run --frozen --offline python -m pytest -q tests/live/test_bundle_poller.py tests/live/test_batch_request_counts.py
```

## Acceptance
- For up to five simultaneous fixtures, opening additional panels does not add polling loops. Calls cannot overlap or burst after sleep. A budget stop preserves last-known data and displays the stop reason.

## Error handling and continuation
Ordinary test/code errors are fixed automatically; then continue the next ready task. A missing key/profile/approval/platform is WAITING_INPUT with one precise user instruction, not a request after every task. Run other ready independent tasks meanwhile. Never treat synthetic data as a substitute for a real-case gate. Authentication failure, scope mismatch, secret leakage or an unsafe privilege change is a stop for the affected real source. Quota/runtime limits never auto-expand.

## Rollback
Stop owned source/session if needed, preserve evidence, and revert only this task’s change through a normal reviewed commit. No hard reset, force push, deletion of other work or mutation of old PASS receipts.
