# PB-11 — Implement fixture bindings, market epochs and freshness as a pure reducer

**Legacy mapping:** LV-03
**Dependencies:** PB-05, PB-07, PB-09
**External input gate:** NONE
**Status:** PLANNED — no implementation claim.

## Files owned/modified
- `src/moj_discovery/live_identity.py`
- `src/moj_discovery/market_book.py`
- `src/moj_discovery/live_state.py`
- `tests/live/test_identity.py`
- `tests/live/test_market_state_join.py`

## Interfaces to produce
- `live_identity.py::bind_fixture(provider: ProviderState, operator: OperatorFacts, evidence: BindingEvidence) -> FixtureBinding`
- `market_book.py::assemble_book(capture: CapturedFields, profile: ExtractionProfile) -> MarketBook`
- `live_state.py::reduce_live_event(current: MatchView, event: LiveEvent) -> MatchView`

## Read before implementation
A3; R4 in `SOURCES.md`; the relevant specification in02..06. File paths are repo-relative, not paths in this plan package. Future paths are created by their owner before verification, not required at PB-00.

## Test-first execution
- [ ] Write focused positive/rejection tests in the exact test files below. A first red must fail because the intended behavior is wrong/missing, not an unrelated import/configuration error.
- [ ] No fuzzyautoapproval;compare ID/date/league/homeaway exactly against accepted mapping. Stable logical fixture key includes providerIDandbindingrevision.
- [ ] Implement status and freshness policies from05_OPERATOR; stale receipt clocks never become upstream freshness. Timer-only updates change displayage notsourcequality.
- [ ] Goal/card/period/VAR/suspension/gap/document change invalidates old books; reopening needs realnewcapture+newproviderobservation+coherentfields, missingoperatorcontext staysUNVERIFIED.
- [ ] Tests H2same score confusion, halftime baseline missing, goal reversal,5fixture independence, partial book and old capture delivered afterbindchange.
- [ ] Run all owned tests and affected regression checks. Inspect actual outputs and diff; preserve failed evidence. Fix code rather than change expected values to get green.
- [ ] Checkpoint task ID, code/config hashes, command exits and actual evidence in `.local/part-b/task-evidence/PB-11.json`; update `docs/execution/PART_B_PROGRESS.md`. Commit only task files under explicit local-commit authority; never push.

## Behavioral assertions to implement in owned tests
These are specification assertions; helper fixtures belong to this task’s test files. They are not an assertion that functions or a live data set already exist.

```text
assert reduce_live_event(view, goal_correction).market_eligible is False
assert view.source_age is None  # missing provider update timestamp
assert h2_score(ht=(2,0), current=(2,1)) == (0,1)
```

## Exact verification commands
Commands run from repo root after owning tasks exist. Commands needing real data or user secret run only at the external gate, in the user terminal.

```bash
uv run --frozen --offline python -m pytest -q tests/live/test_identity.py tests/live/test_market_state_join.py
```

## Acceptance
- Coherence preserved; all mismatches visible; no probability/EV output; one fixture failure does not silently refresh another.

## Error handling and continuation
Ordinary test/code errors are fixed automatically; then continue the next ready task. A missing key/profile/approval/platform is WAITING_INPUT with one precise user instruction, not a request after every task. Run other ready independent tasks meanwhile. Never treat synthetic data as a substitute for a real-case gate. Authentication failure, scope mismatch, secret leakage or an unsafe privilege change is a stop for the affected real source. Quota/runtime limits never auto-expand.

## Rollback
Stop owned source/session if needed, preserve evidence, and revert only this task’s change through a normal reviewed commit. No hard reset, force push, deletion of other work or mutation of old PASS receipts.
