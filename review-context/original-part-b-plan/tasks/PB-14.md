# PB-14 — Implement configuration wizard, scope preview and user-confirmed run intents

**Legacy mapping:** LV-02/04
**Dependencies:** PB-02, PB-03, PB-12
**External input gate:** NONE
**Status:** PLANNED — no implementation claim.

## Files owned/modified
- `tools/configure_live_batched.py`
- `tools/prepare_part_b_intent.py`
- `src/moj_discovery/live_intent.py`
- `tools/probe_football_provider.py`
- `tests/live/test_run_intents.py`
- `tests/live/test_configure_live.py`
- `.gitignore`

## Interfaces to produce
- `live_intent.py::consume_user_intent(intent: RunIntent, config: LiveConfig, confirmation: str) -> RunIntentReceipt`
- `tools/configure_live_batched.py::main(argv: list[str] | None = None) -> int`
- `tools/probe_football_provider.py::run_probe(config: LiveConfig, secret: SecretValue, intent: RunIntentReceipt) -> ProbeResult`

## Read before implementation
A3; P1; R3 in `SOURCES.md`; the relevant specification in02..06. File paths are repo-relative, not paths in this plan package. Future paths are created by their owner before verification, not required at PB-00.

## Test-first execution
- [ ] Write focused positive/rejection tests in the exact test files below. A first red must fail because the intended behavior is wrong/missing, not an unrelated import/configuration error.
- [ ] The wizard requests only nonsecret platform, league, season, fixture and budget inputs. Create a disabled local config. No hardcoded production fixture IDs. Do not overwrite a prior local config without explicit choice.
- [ ] Create source/config-bound, one-use intents. The user terminal previews scope and cost before typed confirmation. This is local user consent, not an independent security signature.
- [ ] Connect the key launcher’s probe/live-readonly enum to the corresponding implementation. A probe uses only status, coverage, bundle and at most two specifically budgeted event comparisons, within20attempts/5minutes.
- [ ] Break the authorization bootstrap cycle as specified in06_EVIDENCE_AND_RUNS.md. Add gitignore entries for local config and evidence; never treat a generated fixture receipt as real permission.
- [ ] Run all owned tests and affected regression checks. Inspect actual outputs and diff; preserve failed evidence. Fix code rather than change expected values to get green.
- [ ] Checkpoint task ID, code/config hashes, command exits and actual evidence in `.local/part-b/task-evidence/PB-14.json`; update `docs/execution/PART_B_PROGRESS.md`. Commit only task files under explicit local-commit authority; never push.

## Behavioral assertions to implement in owned tests
These are specification assertions; helper fixtures belong to this task’s test files. They are not an assertion that functions or a live data set already exist.

```text
with pytest.raises(ValueError):
    consume_user_intent(used_intent, config, "ALLOW PROVIDER PROBE")
assert mock_http.attempts <= 20
assert "API_FOOTBALL_KEY=" not in generated_commands
```

## Exact verification commands
Commands run from repo root after owning tasks exist. Commands needing real data or user secret run only at the external gate, in the user terminal.

```bash
uv run --frozen --offline python -m pytest -q tests/live/test_run_intents.py tests/live/test_configure_live.py
```

## Acceptance
- Declined, expired, replayed or incorrectly source/config-bound intents prevent I/O. The CLI can guide key entry without exposing it. Mock tests enforce the 20-attempt probe cap.

## Error handling and continuation
Ordinary test/code errors are fixed automatically; then continue the next ready task. A missing key/profile/approval/platform is WAITING_INPUT with one precise user instruction, not a request after every task. Run other ready independent tasks meanwhile. Never treat synthetic data as a substitute for a real-case gate. Authentication failure, scope mismatch, secret leakage or an unsafe privilege change is a stop for the affected real source. Quota/runtime limits never auto-expand.

## Rollback
Stop owned source/session if needed, preserve evidence, and revert only this task’s change through a normal reviewed commit. No hard reset, force push, deletion of other work or mutation of old PASS receipts.

## Additional exact assets owned by PB-14
Copy `assets/contracts/live_readonly/v1/run-intent.schema.json` and `intent-store.sql` to their identical repo-relative `contracts/live_readonly/v1/` paths. The local consumption database is `.local/part-b/intent-consumptions.sqlite3`. The trusted user-terminal launcher validates source/config/expiry and confirmation, then commits one INSERT under BEGIN IMMEDIATE before the authorized operation. Duplicate intent ID/hash or run ID rejects; a crash after consumption burns the intent, requiring a fresh user confirmation. This records local consent, not an independent security signature.
