# PB-01 — Create source-owned live contracts, v2 config and fixed endpoint/fixture registries

**Legacy mapping:** LV-02/03
**Dependencies:** PB-00
**External input gate:** NONE
**Status:** PLANNED — no implementation claim.

## Files owned/modified
- `contracts/live_readonly/v1/config.schema.json`
- `contracts/live_readonly/v1/records.schema.json`
- `contracts/live_readonly/v1/wire.schema.json`
- `config/live-batched.example.json`
- `src/moj_discovery/live_contracts.py`
- `src/moj_discovery/live_config.py`
- `tests/live/conftest.py`
- `tests/live/test_live_contracts.py`
- `tests/live/test_live_config.py`
- `extension/src/live/contracts.ts`
- `extension/tsconfig.live.json`

## Interfaces to produce
- `live_contracts.py::validate_live_record(value: object, kind: str) -> dict[str, object]`
- `live_config.py::load_live_config(path: Path, require_enabled: bool = False) -> LiveConfig`
- `tests/live/conftest.py fixtures: synthetic_provider_response, fake_clock, disabled_live_config`
- `extension/src/live/contracts.ts#validateLiveRecord`

## Read before implementation
R2; R4; A3; P5 in `SOURCES.md`; the relevant specification in02..06. File paths are repo-relative, not paths in this plan package. Future paths are created by their owner before verification, not required at PB-00.

## Test-first execution
- [ ] Write focused positive/rejection tests in the exact test files below. A first red must fail because the intended behavior is wrong/missing, not an unrelated import/configuration error.
- [ ] Copy plan assets to exact source-owned paths, not vendor. Add strict local schema registry with no remote reference resolver. Live payload union must have closed branches.
- [ ] Create disabled v2 example with batching and600/run cap. Keep v1 parser/config; migration is explicit configure_live_batched later, old15s values are not forced into oldmin30 validator.
- [ ] Define core types exactly from04_LIVE_CONTRACTS; synthetic test responses labelledSYNTHETIC. All paths/results reflect source kind. Nulls for unsupplied real IDs do not authorize enabled config.
- [ ] Create separate TS build config including live source/tests; do not use compile-only as acceptance. Add config errors for predictions/odds URLs, unbounded fixture list, permissive host, key in config, mixed wire types.
- [ ] Run all owned tests and affected regression checks. Inspect actual outputs and diff; preserve failed evidence. Fix code rather than change expected values to get green.
- [ ] Checkpoint task ID, code/config hashes, command exits and actual evidence in `.local/part-b/task-evidence/PB-01.json`; update `docs/execution/PART_B_PROGRESS.md`. Commit only task files under explicit local-commit authority; never push.

## Behavioral assertions to implement in owned tests
These are specification assertions; helper fixtures belong to this task’s test files. They are not an assertion that functions or a live data set already exist.

```text
assert draft["schema_version"] == "bh-live-readonly-config/v2"
assert draft["provider"]["events_fallback_enabled"] is False
with pytest.raises(ValueError):
    validate_live_record({"payload": {"cookie": "TEST_ONLY"}}, "MarketBook")
```

## Exact verification commands
Commands run from repo root after owning tasks exist. Commands needing real data or user secret run only at the external gate, in the user terminal.

```bash
uv run --frozen --offline python -m pytest -q tests/live/test_live_contracts.py tests/live/test_live_config.py
```

## Acceptance
- v1 behavior preserved; v2 disabled draft valid; v2 enabled without exact scope/evidence invalid; no generic live payload escape.

## Error handling and continuation
Ordinary test/code errors are fixed automatically; then continue the next ready task. A missing key/profile/approval/platform is WAITING_INPUT with one precise user instruction, not a request after every task. Run other ready independent tasks meanwhile. Never treat synthetic data as a substitute for a real-case gate. Authentication failure, scope mismatch, secret leakage or an unsafe privilege change is a stop for the affected real source. Quota/runtime limits never auto-expand.

## Rollback
Stop owned source/session if needed, preserve evidence, and revert only this task’s change through a normal reviewed commit. No hard reset, force push, deletion of other work or mutation of old PASS receipts.
