# PB-09 — Build an inert extraction-profile validator using synthetic DOM samples

**Legacy mapping:** LV-01
**Dependencies:** PB-01
**External input gate:** NONE
**Status:** PLANNED — no implementation claim.

## Files owned/modified
- `contracts/live_readonly/v1/operator-profile.schema.json`
- `src/moj_discovery/operator_profile.py`
- `tests/live/test_operator_profile.py`
- `tests/live/fixtures/synthetic-match.html`

## Interfaces to produce
- `operator_profile.py::validate_extraction_profile(value: object, evidence: ProfileEvidence) -> ExtractionProfile`

## Read before implementation
R3; C3; C4 in `SOURCES.md`; the relevant specification in02..06. File paths are repo-relative, not paths in this plan package. Future paths are created by their owner before verification, not required at PB-00.

## Test-first execution
- [ ] Write focused positive/rejection tests in the exact test files below. A first red must fail because the intended behavior is wrong/missing, not an unrelated import/configuration error.
- [ ] Use05_OPERATOR profile schema. DRAFT/SYNTHETIC profile can run local tests only; accepted real profile needs actual sample hashes and source-bound review.
- [ ] Restrict selectors to allowed match-root subtree and prohibit account/form/input/contenteditable/iframe readers. No arbitrary evaluate/URLs/commands.
- [ ] Synthetic page declares TEST_ONLY names/IDs; do not publish it as a real Mise-o-jeu parser. Test swapped orientation,missingdraw,wrongmarket,horizon ambiguity,secret-bearing selectors.
- [ ] Materialize field-map and parser interface; leave actual selectors unconfigured pendingPB-19 instead of guessing.
- [ ] Run all owned tests and affected regression checks. Inspect actual outputs and diff; preserve failed evidence. Fix code rather than change expected values to get green.
- [ ] Checkpoint task ID, code/config hashes, command exits and actual evidence in `.local/part-b/task-evidence/PB-09.json`; update `docs/execution/PART_B_PROGRESS.md`. Commit only task files under explicit local-commit authority; never push.

## Behavioral assertions to implement in owned tests
These are specification assertions; helper fixtures belong to this task’s test files. They are not an assertion that functions or a live data set already exist.

```text
assert draft.status == "DRAFT"
with pytest.raises(ValueError):
    activate_profile(draft, target="OPERATOR")
assert reject_selector("input[type=password]")
```

## Exact verification commands
Commands run from repo root after owning tasks exist. Commands needing real data or user secret run only at the external gate, in the user terminal.

```bash
uv run --frozen --offline python -m pytest -q tests/live/test_operator_profile.py
```

## Acceptance
- Synthetic profile validated for mocks; profile with no real evidence cannot activate on operator; field-map semantics explicit.

## Error handling and continuation
Ordinary test/code errors are fixed automatically; then continue the next ready task. A missing key/profile/approval/platform is WAITING_INPUT with one precise user instruction, not a request after every task. Run other ready independent tasks meanwhile. Never treat synthetic data as a substitute for a real-case gate. Authentication failure, scope mismatch, secret leakage or an unsafe privilege change is a stop for the affected real source. Quota/runtime limits never auto-expand.

## Rollback
Stop owned source/session if needed, preserve evidence, and revert only this task’s change through a normal reviewed commit. No hard reset, force push, deletion of other work or mutation of old PASS receipts.
