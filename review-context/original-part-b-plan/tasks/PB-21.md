# PB-21 — Run, record and replay one real football match with FT odds

**Legacy mapping:** LV-06
**Dependencies:** PB-20
**External input gate:** USER_LIVE_ONE_RUN_CONFIRMATION
**Status:** PLANNED — no implementation claim.

## Files owned/modified
- `tools/qualify_live_readonly.py`
- `tests/live/test_live_qualification.py`
- `docs/live/read-only-qualification.md`

## Interfaces to produce
- `tools/qualify_live_readonly.py::qualify_recorded_run(run_dir: Path) -> LiveQualification`

## Read before implementation
A1; A2; C1; P3 in `SOURCES.md`; the relevant specification in02..06. File paths are repo-relative, not paths in this plan package. Future paths are created by their owner before verification, not required at PB-00.

## Test-first execution
- [ ] Write focused positive/rejection tests in the exact test files below. A first red must fail because the intended behavior is wrong/missing, not an unrelated import/configuration error.
- [ ] Execute the explicitly approved bounded live session in the user’s terminal. Start WAITING_FOR_DATA and compare complete visible FT books against recorded IDs/prices at least three times.
- [ ] Record actual provider observations, operator captures, quota usage and stale/conflict states. Respect600attempts and120minutes; no invented update during a source outage.
- [ ] Stop and freeze the run before fresh replay. Rare real shocks not observed remain NOT_OBSERVED; synthetic tests are labelled separately, never substituted for real observation.
- [ ] Issue one-match FT qualification only for the observed scope. H1/H2 availability is separately stated; there are no forecasts or wager actions.
- [ ] Run all owned tests and affected regression checks. Inspect actual outputs and diff; preserve failed evidence. Fix code rather than change expected values to get green.
- [ ] Checkpoint task ID, code/config hashes, command exits and actual evidence in `.local/part-b/task-evidence/PB-21.json`; update `docs/execution/PART_B_PROGRESS.md`. Commit only task files under explicit local-commit authority; never push.

## Behavioral assertions to implement in owned tests
These are specification assertions; helper fixtures belong to this task’s test files. They are not an assertion that functions or a live data set already exist.

```text
assert qualification.observed_fixture_ids == selected_fixture_ids
assert qualification.complete_ft_book_checks >= 3
assert qualification.replay.equal is True
```

## Exact verification commands
Commands run from repo root after owning tasks exist. Commands needing real data or user secret run only at the external gate, in the user terminal.

```bash
uv run --frozen --offline python tools/run_with_api_football_key.py --action live-readonly --config config/live.local.json --intent .local/part-b/intents/live-one.json
uv run --frozen --offline python tools/qualify_live_readonly.py --run-dir .local/part-b/live-one
```

## Acceptance
- The one-match live gate reports an evidence-backed result for its exact scope. Actual data are retained and replayable. Manual stop closes only owned resources.

## Error handling and continuation
Ordinary test/code errors are fixed automatically; then continue the next ready task. A missing key/profile/approval/platform is WAITING_INPUT with one precise user instruction, not a request after every task. Run other ready independent tasks meanwhile. Never treat synthetic data as a substitute for a real-case gate. Authentication failure, scope mismatch, secret leakage or an unsafe privilege change is a stop for the affected real source. Quota/runtime limits never auto-expand.

## Rollback
Stop owned source/session if needed, preserve evidence, and revert only this task’s change through a normal reviewed commit. No hard reset, force push, deletion of other work or mutation of old PASS receipts.
