# PB-00 — Reconcile Part A evidence and register the Part B implementation scope

**Legacy mapping:** OS-15 / all LV
**Dependencies:** none
**External input gate:** NONE
**Status:** PLANNED — no implementation claim.

## Files owned/modified
- `docs/execution/PART_B_SCOPE.md`
- `docs/execution/PART_B_PROGRESS.md`
- `docs/live/part-a-entry-review.md`

## Interfaces to produce
- `PartBProgress record: actual_head, completed_task_ids, current_task_id, blocked_tasks, evidence_refs`

## Read before implementation
R1; R2; R3; R5 in `SOURCES.md`; the relevant specification in02..06. File paths are repo-relative, not paths in this plan package. Future paths are created by their owner before verification, not required at PB-00.

## Test-first execution
- [ ] Write focused positive/rejection tests in the exact test files below. A first red must fail because the intended behavior is wrong/missing, not an unrelated import/configuration error.
- [ ] Read AGENTS and nested instructions, actual HEAD/status. Do not reset to inspected commit. Confirm source pack and vendor unchanged; establish local-commit authority from pasted user scope and host controller.
- [ ] Locate actual OS-15 result and verify with the same browser selection; record evidence or MISSING_PART_A_EVIDENCE. No claim that the chat says completed is a current verifier result.
- [ ] Register code/mock batch and external gates. Preserve root live-access prohibition until separate real-run authority. Check installed Python/Node/pnpm/uv/browser and existing lock. Do not rebuild baseline or upgrade toolchain.
- [ ] Read old LV mapping and v1 configuration. Record this package as the v2 batching/secret/capture seam amendment. Missing final A evidence blocks real activation but not independent mock implementation.
- [ ] Run all owned tests and affected regression checks. Inspect actual outputs and diff; preserve failed evidence. Fix code rather than change expected values to get green.
- [ ] Checkpoint task ID, code/config hashes, command exits and actual evidence in `.local/part-b/task-evidence/PB-00.json`; update `docs/execution/PART_B_PROGRESS.md`. Commit only task files under explicit local-commit authority; never push.

## Behavioral assertions to implement in owned tests
These are specification assertions; helper fixtures belong to this task’s test files. They are not an assertion that functions or a live data set already exist.

```text
assert scope.live_run_authorized is False
assert scope.money_authorized is False
```

## Exact verification commands
Commands run from repo root after owning tasks exist. Commands needing real data or user secret run only at the external gate, in the user terminal.

```bash
git diff --check
```

## Acceptance
- Source/evidence status recorded without any network/session/API call; worktree conflicts isolated; next ready task automatically selected.

## Error handling and continuation
Ordinary test/code errors are fixed automatically; then continue the next ready task. A missing key/profile/approval/platform is WAITING_INPUT with one precise user instruction, not a request after every task. Run other ready independent tasks meanwhile. Never treat synthetic data as a substitute for a real-case gate. Authentication failure, scope mismatch, secret leakage or an unsafe privilege change is a stop for the affected real source. Quota/runtime limits never auto-expand.

## Rollback
Stop owned source/session if needed, preserve evidence, and revert only this task’s change through a normal reviewed commit. No hard reset, force push, deletion of other work or mutation of old PASS receipts.
