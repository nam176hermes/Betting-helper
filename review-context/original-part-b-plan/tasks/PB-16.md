# PB-16 — Implement live preflight evidence admission and executable security checks

**Legacy mapping:** LV-04/06
**Dependencies:** PB-12, PB-14, PB-15
**External input gate:** NONE
**Status:** PLANNED — no implementation claim.

## Files owned/modified
- `src/moj_discovery/live_preflight_batched.py`
- `tools/live_preflight_batched.py`
- `tests/live/test_live_preflight_batched.py`
- `tests/live/test_live_security.py`
- `tools/verify_part_b.py`
- `docs/live/pre-live-security-review.md`

## Interfaces to produce
- `live_preflight_batched.py::evaluate_live_readiness(config: LiveConfig, verified: EvidenceIndex, key_present: bool) -> Readiness`
- `tools/verify_part_b.py::verify_profile(profile: str, output: Path) -> VerificationResult`

## Read before implementation
R3; P1; C4 in `SOURCES.md`; the relevant specification in02..06. File paths are repo-relative, not paths in this plan package. Future paths are created by their owner before verification, not required at PB-00.

## Test-first execution
- [ ] Write focused positive/rejection tests in the exact test files below. A first red must fail because the intended behavior is wrong/missing, not an unrelated import/configuration error.
- [ ] Preserve the legacy v1 preflight. The v2 verifier consumes actual provider/profile/platform/review/intent evidence, not truthy claims. Key presence means PRESENT_NOT_AUTHENTICATED.
- [ ] Implement the security case matrix: redirects/proxy, secret leakage, permission/module graph, fixed read-only DOM operations, wire roles and unexpected outbound destinations.
- [ ] Validate artifact references and current source/config/profile hashes. Missing or synthetic evidence produces named pending conditions. A changed enabled dependency requires its tests and review again.
- [ ] Report self-review as SELF_ONLY. Respect existing real host approval mechanisms; do not fabricate a signer or claim that two run-ID strings prove reviewer independence.
- [ ] Run all owned tests and affected regression checks. Inspect actual outputs and diff; preserve failed evidence. Fix code rather than change expected values to get green.
- [ ] Checkpoint task ID, code/config hashes, command exits and actual evidence in `.local/part-b/task-evidence/PB-16.json`; update `docs/execution/PART_B_PROGRESS.md`. Commit only task files under explicit local-commit authority; never push.

## Behavioral assertions to implement in owned tests
These are specification assertions; helper fixtures belong to this task’s test files. They are not an assertion that functions or a live data set already exist.

```text
result = evaluate_live_readiness(config, synthetic_evidence, key_present=True)
assert result.live_read_only_ready is False
assert "REQUIRES_REAL_PROVIDER_EVIDENCE" in result.missing_inputs
```

## Exact verification commands
Commands run from repo root after owning tasks exist. Commands needing real data or user secret run only at the external gate, in the user terminal.

```bash
uv run --frozen --offline python -m pytest -q tests/live/test_live_preflight_batched.py tests/live/test_live_security.py
```

## Acceptance
- The pure preflight decision performs no network I/O. Forged, synthetic or stale evidence cannot qualify a real run. The verification CLI exists and is tested before the first provider probe.

## Error handling and continuation
Ordinary test/code errors are fixed automatically; then continue the next ready task. A missing key/profile/approval/platform is WAITING_INPUT with one precise user instruction, not a request after every task. Run other ready independent tasks meanwhile. Never treat synthetic data as a substitute for a real-case gate. Authentication failure, scope mismatch, secret leakage or an unsafe privilege change is a stop for the affected real source. Quota/runtime limits never auto-expand.

## Rollback
Stop owned source/session if needed, preserve evidence, and revert only this task’s change through a normal reviewed commit. No hard reset, force push, deletion of other work or mutation of old PASS receipts.
