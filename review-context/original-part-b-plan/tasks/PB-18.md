# PB-18 — Guide user key entry, confirm scope and execute the bounded real provider probe

**Legacy mapping:** LV-02
**Dependencies:** PB-17
**External input gate:** USER_SECRET_AND_PROVIDER_PROBE_APPROVAL
**Status:** PLANNED — no implementation claim.

## Files owned/modified
- `docs/live/provider-feasibility.md`
- `docs/execution/PART_B_PROGRESS.md`

## Interfaces to produce
- `ProbeResult: source_kind=OBSERVED_REAL; selected fixture IDs; projected quota metadata; HTTP attempt count; embedded-section coverage; timing limitations.`

## Read before implementation
A1; A2; A3; P1 in `SOURCES.md`; the relevant specification in02..06. File paths are repo-relative, not paths in this plan package. Future paths are created by their owner before verification, not required at PB-00.

## Test-first execution
- [ ] Write focused positive/rejection tests in the exact test files below. A first red must fail because the intended behavior is wrong/missing, not an unrelated import/configuration error.
- [ ] Send the Vietnamese instructions from 03_KEY_GUIDE_VI.md, including the user-owned WSL terminal command. Never request the key value in chat or prompt for it in an agent-captured terminal.
- [ ] The user chooses league/season/fixture and confirms the 20-attempt / 5-minute probe. Codex reads sanitized result metadata, not the credential. An absent key becomes WAITING_FOR_USER_SECRET.
- [ ] Validate actual account quota, competition coverage and bundled details for selected fixtures. On auth failure stop. Never buy or upgrade an account automatically.
- [ ] Only if section quality needs comparison, use at most two approved fallback calls within the same budget. Record actual timing/missingness; do not turn fallback polling on automatically.
- [ ] Run all owned tests and affected regression checks. Inspect actual outputs and diff; preserve failed evidence. Fix code rather than change expected values to get green.
- [ ] Checkpoint task ID, code/config hashes, command exits and actual evidence in `.local/part-b/task-evidence/PB-18.json`; update `docs/execution/PART_B_PROGRESS.md`. Commit only task files under explicit local-commit authority; never push.

## Behavioral assertions to implement in owned tests
These are specification assertions; helper fixtures belong to this task’s test files. They are not an assertion that functions or a live data set already exist.

```text
assert probe.source_kind == "OBSERVED_REAL"
assert probe.http_attempts <= 20
assert "account" not in probe.public_report and "key" not in probe.public_report
```

## Exact verification commands
Commands run from repo root after owning tasks exist. Commands needing real data or user secret run only at the external gate, in the user terminal.

```bash
uv run --frozen --offline python tools/run_with_api_football_key.py --action probe --config config/live.local.json --intent .local/part-b/intents/provider-probe.json
```

## Acceptance
- The direct API credential is authenticated without disclosure. Actual quota and selected competition/fixture coverage are observed. This result does not approve operator access.

## Error handling and continuation
Ordinary test/code errors are fixed automatically; then continue the next ready task. A missing key/profile/approval/platform is WAITING_INPUT with one precise user instruction, not a request after every task. Run other ready independent tasks meanwhile. Never treat synthetic data as a substitute for a real-case gate. Authentication failure, scope mismatch, secret leakage or an unsafe privilege change is a stop for the affected real source. Quota/runtime limits never auto-expand.

## Rollback
Stop owned source/session if needed, preserve evidence, and revert only this task’s change through a normal reviewed commit. No hard reset, force push, deletion of other work or mutation of old PASS receipts.
