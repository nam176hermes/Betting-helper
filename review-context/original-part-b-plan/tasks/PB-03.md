# PB-03 — Implement global attempt reservation, shared quota ledger and estimator

**Legacy mapping:** LV-02
**Dependencies:** PB-01
**External input gate:** NONE
**Status:** PLANNED — no implementation claim.

## Files owned/modified
- `contracts/live_readonly/v1/quota-store.sql`
- `src/moj_discovery/providers/quota.py`
- `src/moj_discovery/providers/poll_budget.py`
- `tests/live/test_provider_budget.py`
- `tests/live/test_quota_concurrency.py`

## Interfaces to produce
- `quota.py::QuotaLedger.reserve_attempt(scope_id: str, purpose: str, now_utc: datetime, now_mono: float) -> Reservation | Denied`
- `quota.py::QuotaLedger.finalize_attempt(reservation_id: str, outcome: str, headers: Mapping[str, str]) -> None`
- `poll_budget.py::estimate_requests(segments: Sequence[PollSegment], setup: int, confirmations: int, fallback_calls: int, reserve_fraction: Decimal) -> BudgetEstimate`

## Read before implementation
A1; A5; P3 in `SOURCES.md`; the relevant specification in02..06. File paths are repo-relative, not paths in this plan package. Future paths are created by their owner before verification, not required at PB-00.

## Test-first execution
- [ ] Write focused positive/rejection tests in the exact test files below. A first red must fail because the intended behavior is wrong/missing, not an unrelated import/configuration error.
- [ ] Use supplied DDL for persistent attempt reservations; one provider slot and process lock. Reservation commit precedes HTTP; unknown/failure is never refunded automatically.
- [ ] Implement6000/day600/session6/sliding60s10s minimum gap andin-flight1. Provider headers reduce available allowance. Renew day only with consistent quota evidence, no wall-clock rollback bypass.
- [ ] Count setup, status, retries, fallback, manual refresh as attempts. Preflight estimates using union of monitor windows, not per-fixture multiplication for simultaneous batch.
- [ ] Test two processes cannot each reserve beyond cap and second process cannot start a parallel poller.500/429/timeout count even when provider billing unknown.
- [ ] Run all owned tests and affected regression checks. Inspect actual outputs and diff; preserve failed evidence. Fix code rather than change expected values to get green.
- [ ] Checkpoint task ID, code/config hashes, command exits and actual evidence in `.local/part-b/task-evidence/PB-03.json`; update `docs/execution/PART_B_PROGRESS.md`. Commit only task files under explicit local-commit authority; never push.

## Behavioral assertions to implement in owned tests
These are specification assertions; helper fixtures belong to this task’s test files. They are not an assertion that functions or a live data set already exist.

```text
assert budget_for_120min_constant15s == 485
assert reserve20percent_budget == 582
assert quota.seventh_attempt_in_60_seconds_allowed is False
```

## Exact verification commands
Commands run from repo root after owning tasks exist. Commands needing real data or user secret run only at the external gate, in the user terminal.

```bash
uv run --frozen --offline python -m pytest -q tests/live/test_provider_budget.py tests/live/test_quota_concurrency.py
```

## Acceptance
- The arithmetic oracle returns 480 periodic calls + 5 overhead = 485 and 582 with a 20% retry reserve. Five concurrent fixtures share that total; five sequential windows total 2,425. Five event fallbacks add 600 calls and exceed the default session cap.

## Error handling and continuation
Ordinary test/code errors are fixed automatically; then continue the next ready task. A missing key/profile/approval/platform is WAITING_INPUT with one precise user instruction, not a request after every task. Run other ready independent tasks meanwhile. Never treat synthetic data as a substitute for a real-case gate. Authentication failure, scope mismatch, secret leakage or an unsafe privilege change is a stop for the affected real source. Quota/runtime limits never auto-expand.

## Rollback
Stop owned source/session if needed, preserve evidence, and revert only this task’s change through a normal reviewed commit. No hard reset, force push, deletion of other work or mutation of old PASS receipts.
