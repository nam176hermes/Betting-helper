# PB-10 — Implement fixed DOM reader, complete book assembly and document identity guards

**Legacy mapping:** LV-01/04
**Dependencies:** PB-08, PB-09
**External input gate:** NONE
**Status:** PLANNED — no implementation claim.

## Files owned/modified
- `extension/src/live/dom_reader.ts`
- `extension/src/live/capture.ts`
- `extension/src/live/background.ts`
- `extension/manifest.live.json`
- `extension/test/live/capture.test.ts`
- `tests/live/test_capture_graph.py`

## Interfaces to produce
- `extension/src/live/dom_reader.ts#readVisibleBook`
- `extension/src/live/capture.ts#startReadOnlyCapture`
- `extension/src/live/background.ts#handleCaptureMessage`

## Read before implementation
C1; C2; C3; C4 in `SOURCES.md`; the relevant specification in02..06. File paths are repo-relative, not paths in this plan package. Future paths are created by their owner before verification, not required at PB-00.

## Test-first execution
- [ ] Write focused positive/rejection tests in the exact test files below. A first red must fail because the intended behavior is wrong/missing, not an unrelated import/configuration error.
- [ ] Initial live manifest uses minimal DOM permissions from05_OPERATOR, no debugger/cookies/network interception. Fixed bundled file injection only, frame0andISOLATED.
- [ ] Read all3 prices/labels/context in one DOM task; stable2reads100ms;debounce250ms;max1emit/2s;watchdog30s onlyvisible. Expose incomplete/stale not merged book.
- [ ] Bind response sender tab/document/profile/binding revision. Route replacement/hidden tab/root replacement invalidates prior eligibility. Optional host permission requested by user action only.
- [ ] Tests use real isolated synthetic browser. Exclude secret areas, prohibit clicks/POST/Runtime/Input/cookies, validate unknown graph imports. No authenticated tab here.
- [ ] Run all owned tests and affected regression checks. Inspect actual outputs and diff; preserve failed evidence. Fix code rather than change expected values to get green.
- [ ] Checkpoint task ID, code/config hashes, command exits and actual evidence in `.local/part-b/task-evidence/PB-10.json`; update `docs/execution/PART_B_PROGRESS.md`. Commit only task files under explicit local-commit authority; never push.

## Behavioral assertions to implement in owned tests
These are specification assertions; helper fixtures belong to this task’s test files. They are not an assertion that functions or a live data set already exist.

```text
assert readVisibleBook(hidden_market).status == "NOT_VISIBLE"
assert onMessage(late_tab_a, active_tab_b).accepted is False
assert live_permissions.disjoint({"debugger", "cookies"})
```

## Exact verification commands
Commands run from repo root after owning tasks exist. Commands needing real data or user secret run only at the external gate, in the user terminal.

```bash
pnpm --dir extension exec tsc -p tsconfig.test.json
node --test extension/.test-build/test/live/capture.test.js
uv run --frozen --offline python -m pytest -q tests/live/test_capture_graph.py
```

## Acceptance
- Synthetic capture tests pass. No operator mutation capability exists. Unavailable or hidden markets display MARKET_NOT_VISIBLE rather than triggering a click or guessed value. Late output from a replaced document is rejected.

## Error handling and continuation
Ordinary test/code errors are fixed automatically; then continue the next ready task. A missing key/profile/approval/platform is WAITING_INPUT with one precise user instruction, not a request after every task. Run other ready independent tasks meanwhile. Never treat synthetic data as a substitute for a real-case gate. Authentication failure, scope mismatch, secret leakage or an unsafe privilege change is a stop for the affected real source. Quota/runtime limits never auto-expand.

## Rollback
Stop owned source/session if needed, preserve evidence, and revert only this task’s change through a normal reviewed commit. No hard reset, force push, deletion of other work or mutation of old PASS receipts.
