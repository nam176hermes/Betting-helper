# PB-02 — Build a user-operated no-echo API key launcher and secret handling tests

**Legacy mapping:** LV-02
**Dependencies:** PB-01
**External input gate:** NONE
**Status:** PLANNED — no implementation claim.

## Files owned/modified
- `src/moj_discovery/secrets_local.py`
- `tools/run_with_api_football_key.py`
- `tests/live/test_secret_entry.py`
- `tests/live/test_secret_nonleakage.py`
- `docs/runbooks/API_FOOTBALL_KEY.md`

## Interfaces to produce
- `secrets_local.py::SecretValue (repr/str redacted; reveal_for_header() -> str)`
- `secrets_local.py::obtain_api_football_key(interactive: bool) -> SecretValue`
- `tools/run_with_api_football_key.py::main(argv: list[str] | None = None) -> int`

## Read before implementation
P1; A3; R3 in `SOURCES.md`; the relevant specification in02..06. File paths are repo-relative, not paths in this plan package. Future paths are created by their owner before verification, not required at PB-00.

## Test-first execution
- [ ] Write focused positive/rejection tests in the exact test files below. A first red must fail because the intended behavior is wrong/missing, not an unrelated import/configuration error.
- [ ] Implement no-echo getpass in a user-controlled TTY, GetPassWarning treated as error. NoTTY/empty/CRLF reject; accept provider key shape without assuming32hex.
- [ ] Launcher action enum probe/live-readonly; parse config/intent, display exact scope and typed confirmation before obtaining key. Import dispatcher at action runtime only; no arbitrary subprocess command.
- [ ] Write Vietnamese user instructions exactly as03_KEY_GUIDE_VI. Do not request real key while coding this task. Mock client/key and verify only finite output statuses.
- [ ] Block real key from logs, argv, configs, API error messages, signed reports, browser messages and child env. Optional existing env reads only exact name; no global config or .env write.
- [ ] Run all owned tests and affected regression checks. Inspect actual outputs and diff; preserve failed evidence. Fix code rather than change expected values to get green.
- [ ] Checkpoint task ID, code/config hashes, command exits and actual evidence in `.local/part-b/task-evidence/PB-02.json`; update `docs/execution/PART_B_PROGRESS.md`. Commit only task files under explicit local-commit authority; never push.

## Behavioral assertions to implement in owned tests
These are specification assertions; helper fixtures belong to this task’s test files. They are not an assertion that functions or a live data set already exist.

```text
secret = SecretValue("TEST_ONLY_credential")
assert repr(secret) == "[REDACTED]"
assert "TEST_ONLY_credential" not in captured_logs
assert fake_http.calls == []  # declined confirmation / no TTY
```

## Exact verification commands
Commands run from repo root after owning tasks exist. Commands needing real data or user secret run only at the external gate, in the user terminal.

```bash
uv run --frozen --offline python -m pytest -q tests/live/test_secret_entry.py tests/live/test_secret_nonleakage.py
```

## Acceptance
- All mock secret leakage tests pass; aborted/noTTY prompt emits zero HTTP attempts; runtime action requiring unfinished provider owner cannot report success.

## Error handling and continuation
Ordinary test/code errors are fixed automatically; then continue the next ready task. A missing key/profile/approval/platform is WAITING_INPUT with one precise user instruction, not a request after every task. Run other ready independent tasks meanwhile. Never treat synthetic data as a substitute for a real-case gate. Authentication failure, scope mismatch, secret leakage or an unsafe privilege change is a stop for the affected real source. Quota/runtime limits never auto-expand.

## Rollback
Stop owned source/session if needed, preserve evidence, and revert only this task’s change through a normal reviewed commit. No hard reset, force push, deletion of other work or mutation of old PASS receipts.
