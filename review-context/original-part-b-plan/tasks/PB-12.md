# PB-12 — Connect the shared live service and projection subscriptions with mocks

**Legacy mapping:** LV-04
**Dependencies:** PB-06, PB-08, PB-10, PB-11
**External input gate:** NONE
**Status:** PLANNED — no implementation claim.

## Files owned/modified
- `src/moj_discovery/live_service.py`
- `tools/run_live_readonly.py`
- `tests/live/test_live_service.py`

## Interfaces to produce
- `live_service.py::run_live_service(config: LiveConfig, secret: SecretValue, admitted: RunAdmission) -> LiveRunResult`
- `tools/run_live_readonly.py::main(argv: list[str] | None = None) -> int`

## Read before implementation
A1; P4; C2 in `SOURCES.md`; the relevant specification in02..06. File paths are repo-relative, not paths in this plan package. Future paths are created by their owner before verification, not required at PB-00.

## Test-first execution
- [ ] Write focused positive/rejection tests in the exact test files below. A first red must fail because the intended behavior is wrong/missing, not an unrelated import/configuration error.
- [ ] The backend owns one poller, store and wire server per run. Provider observations and capture frames enter separately validated pipelines before the pure reducer. Opening another UI must not create another poller.
- [ ] Require a verified RunAdmission. Unit tests explicitly use MOCK admission and injected network-denied transports. Never add a force flag or a success-by-default preflight.
- [ ] Implement manual stop, scope expiry, quota/lease exhaustion and visibly stale disconnected sources. Backend restart requires a fresh pairing and user-run key process. Close only owned resources.
- [ ] Test missing sources, multiple subscribers, malformed input, replay and API-key exclusion from browser/process messages.
- [ ] Run all owned tests and affected regression checks. Inspect actual outputs and diff; preserve failed evidence. Fix code rather than change expected values to get green.
- [ ] Checkpoint task ID, code/config hashes, command exits and actual evidence in `.local/part-b/task-evidence/PB-12.json`; update `docs/execution/PART_B_PROGRESS.md`. Commit only task files under explicit local-commit authority; never push.

## Behavioral assertions to implement in owned tests
These are specification assertions; helper fixtures belong to this task’s test files. They are not an assertion that functions or a live data set already exist.

```text
service.open_panel(); service.open_panel(); service.open_workspace()
assert service.provider_poller_count == 1
assert service.initial_view.status == "WAITING_FOR_DATA"
```

## Exact verification commands
Commands run from repo root after owning tasks exist. Commands needing real data or user secret run only at the external gate, in the user terminal.

```bash
uv run --frozen --offline python -m pytest -q tests/live/test_live_service.py
```

## Acceptance
- The mock end-to-end live-contract path works. Real startup remains blocked until PB-20 admits the required evidence. The UI and extension never receive the API key.

## Error handling and continuation
Ordinary test/code errors are fixed automatically; then continue the next ready task. A missing key/profile/approval/platform is WAITING_INPUT with one precise user instruction, not a request after every task. Run other ready independent tasks meanwhile. Never treat synthetic data as a substitute for a real-case gate. Authentication failure, scope mismatch, secret leakage or an unsafe privilege change is a stop for the affected real source. Quota/runtime limits never auto-expand.

## Rollback
Stop owned source/session if needed, preserve evidence, and revert only this task’s change through a normal reviewed commit. No hard reset, force push, deletion of other work or mutation of old PASS receipts.
