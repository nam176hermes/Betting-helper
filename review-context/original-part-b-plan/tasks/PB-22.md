# PB-22 — Scale accepted watchlist from one to three then five concurrent matches

**Legacy mapping:** LV-05/06
**Dependencies:** PB-21
**External input gate:** USER_EXPANDED_SCOPE_RUN_CONFIRMATION
**Status:** PLANNED — no implementation claim.

## Files owned/modified
- `tests/live/test_multimatch_qualification.py`
- `docs/live/multimatch-qualification.md`
- `tools/qualify_live_readonly.py`

## Interfaces to produce
- `LiveQualification.scope.max_matches in {1,3,5}; stages separately admitted`

## Read before implementation
A1; C2 in `SOURCES.md`; the relevant specification in02..06. File paths are repo-relative, not paths in this plan package. Future paths are created by their owner before verification, not required at PB-00.

## Test-first execution
- [ ] Write focused positive/rejection tests in the exact test files below. A first red must fail because the intended behavior is wrong/missing, not an unrelated import/configuration error.
- [ ] Run three fixtures with a new user-approved config/budget/intent, qualify and replay, then do the same for five. API capacity20 is not permission to monitor20.
- [ ] Measure shared cohort calls, missing fixture behavior, capture isolation and selected IDs. If simultaneous real fixtures are unavailable, report WAITING_MATCH_WINDOW.
- [ ] Opening panels and changing the active fixture must not create extra pollers. Test hidden/closed tabs only with consent; the relevant book becomes stale without contaminating others.
- [ ] Keep fallback OFF unless its profile is admitted. Budget exhaustion stops or pauses visibly; no automatic cap increase.
- [ ] Run all owned tests and affected regression checks. Inspect actual outputs and diff; preserve failed evidence. Fix code rather than change expected values to get green.
- [ ] Checkpoint task ID, code/config hashes, command exits and actual evidence in `.local/part-b/task-evidence/PB-22.json`; update `docs/execution/PART_B_PROGRESS.md`. Commit only task files under explicit local-commit authority; never push.

## Behavioral assertions to implement in owned tests
These are specification assertions; helper fixtures belong to this task’s test files. They are not an assertion that functions or a live data set already exist.

```text
assert one_batch.ids == sorted(selected_five_ids)
assert actual_request_count == backend_ledger_attempt_count
assert model_enabled is False
```

## Exact verification commands
Commands run from repo root after owning tasks exist. Commands needing real data or user secret run only at the external gate, in the user terminal.

```bash
uv run --frozen --offline python -m pytest -q tests/live/test_multimatch_qualification.py
```

## Acceptance
- Three- and five-fixture scopes are measured and admitted separately. Request cost follows shared polling windows, not UI widget count. Each fixture retains independent coherence state.

## Error handling and continuation
Ordinary test/code errors are fixed automatically; then continue the next ready task. A missing key/profile/approval/platform is WAITING_INPUT with one precise user instruction, not a request after every task. Run other ready independent tasks meanwhile. Never treat synthetic data as a substitute for a real-case gate. Authentication failure, scope mismatch, secret leakage or an unsafe privilege change is a stop for the affected real source. Quota/runtime limits never auto-expand.

## Rollback
Stop owned source/session if needed, preserve evidence, and revert only this task’s change through a normal reviewed commit. No hard reset, force push, deletion of other work or mutation of old PASS receipts.
