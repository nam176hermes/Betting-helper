# PB-20 — Bind real sources, review enabled surfaces and admit a single-fixture run

**Legacy mapping:** LV-03/04
**Dependencies:** PB-18, PB-19
**External input gate:** CURRENT_INDEPENDENT_SECURITY_REVIEW_AND_RUN_SELECTION
**Status:** PLANNED — no implementation claim.

## Files owned/modified
- `docs/live/live-admission-review.md`
- `tests/live/test_admitted_live_bindings.py`
- `docs/execution/PART_B_PROGRESS.md`

## Interfaces to produce
- `RunAdmission: verified current source, config, profile, provider, platform and security evidence references; exact fixture/budget scope; no monetary authority.`

## Read before implementation
R3; A1; W1 in `SOURCES.md`; the relevant specification in02..06. File paths are repo-relative, not paths in this plan package. Future paths are created by their owner before verification, not required at PB-00.

## Test-first execution
- [ ] Write focused positive/rejection tests in the exact test files below. A first red must fail because the intended behavior is wrong/missing, not an unrelated import/configuration error.
- [ ] Bind the selected provider fixture to the observed operator fixture using date, league, participants and verified orientation. No name-fuzzy auto-approval.
- [ ] Run regressions with real sanitized provider/profile samples. Fix actual mismatches without weakening correction, missingness or complete-book requirements.
- [ ] Obtain an actual independent read-only review for the enabled surface. Missing review is WAITING_REVIEW. Use existing host receipts when required, without manufacturing them.
- [ ] Run preflight against current references and recomputed budgets. Prepare the one-match intent, which the user must confirm separately before live startup.
- [ ] Run all owned tests and affected regression checks. Inspect actual outputs and diff; preserve failed evidence. Fix code rather than change expected values to get green.
- [ ] Checkpoint task ID, code/config hashes, command exits and actual evidence in `.local/part-b/task-evidence/PB-20.json`; update `docs/execution/PART_B_PROGRESS.md`. Commit only task files under explicit local-commit authority; never push.

## Behavioral assertions to implement in owned tests
These are specification assertions; helper fixtures belong to this task’s test files. They are not an assertion that functions or a live data set already exist.

```text
assert admission.fixture_count == 1
assert admission.profile_hash == current_profile_hash
assert admission.model_enabled is False and admission.money_enabled is False
```

## Exact verification commands
Commands run from repo root after owning tasks exist. Commands needing real data or user secret run only at the external gate, in the user terminal.

```bash
uv run --frozen --offline python -m pytest -q tests/live/test_admitted_live_bindings.py
uv run --frozen --offline python tools/live_preflight_batched.py --config config/live.local.json
```

## Acceptance
- One known fixture becomes eligible for an explicitly confirmed bounded read-only run. This is not yet LIVE_READ_ONLY_PASS, and money remains disabled.

## Error handling and continuation
Ordinary test/code errors are fixed automatically; then continue the next ready task. A missing key/profile/approval/platform is WAITING_INPUT with one precise user instruction, not a request after every task. Run other ready independent tasks meanwhile. Never treat synthetic data as a substitute for a real-case gate. Authentication failure, scope mismatch, secret leakage or an unsafe privilege change is a stop for the affected real source. Quota/runtime limits never auto-expand.

## Rollback
Stop owned source/session if needed, preserve evidence, and revert only this task’s change through a normal reviewed commit. No hard reset, force push, deletion of other work or mutation of old PASS receipts.
