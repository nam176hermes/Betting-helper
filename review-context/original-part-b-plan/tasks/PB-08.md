# PB-08 — Add live capture spool and separate authenticated live wire

**Legacy mapping:** LV-04
**Dependencies:** PB-01, PB-07
**External input gate:** NONE
**Status:** PLANNED — no implementation claim.

## Files owned/modified
- `extension/src/storage/durable_idb.ts`
- `extension/src/spool.ts`
- `extension/src/live/spool.ts`
- `extension/src/live/protocol.ts`
- `extension/src/live/transport.ts`
- `src/moj_discovery/live_wire.py`
- `src/moj_discovery/live_receiver.py`
- `tests/live/test_live_wire.py`
- `tests/live/test_live_spool_bridge.py`
- `extension/test/live/spool.test.ts`
- `extension/test/live/wire.test.ts`

## Interfaces to produce
- `extension/src/live/spool.ts#LiveCaptureSpool`
- `extension/src/live/transport.ts#LiveTransport`
- `live_receiver.py::serve_live_receiver(context: LiveRunContext, store: LiveStore, pairing: PairingAuthority) -> LiveReceiver`

## Read before implementation
R4; P3; P4; P5; C2 in `SOURCES.md`; the relevant specification in02..06. File paths are repo-relative, not paths in this plan package. Future paths are created by their owner before verification, not required at PB-00.

## Test-first execution
- [ ] Write focused positive/rejection tests in the exact test files below. A first red must fail because the intended behavior is wrong/missing, not an unrelated import/configuration error.
- [ ] Extract only small shared IDB transaction helper; preserve old Spool semantics and rerun its unit/browser regressions. New live wrapper validates new schema before every write/read.
- [ ] Separate protocol/path/HMAC domain from offline. Implement bounded exact Origin/Host role-aware handshake and counter replay rejection; client accepts ACK only for committed identity+hash.
- [ ] Implement manual 120-second pairing channel user popup/terminal, not API key; no public unauthenticated command listener. One multiplexed capture producer; subscribers get projections only.
- [ ] Test browser worker restart, receiver commit-before-ACK loss, retained spool resend, wrong origin/key/counter, offline message on live path, producer attempting UI role and subscriber attempting append. Test hooks absent from eventual live build.
- [ ] Run all owned tests and affected regression checks. Inspect actual outputs and diff; preserve failed evidence. Fix code rather than change expected values to get green.
- [ ] Checkpoint task ID, code/config hashes, command exits and actual evidence in `.local/part-b/task-evidence/PB-08.json`; update `docs/execution/PART_B_PROGRESS.md`. Commit only task files under explicit local-commit authority; never push.

## Behavioral assertions to implement in owned tests
These are specification assertions; helper fixtures belong to this task’s test files. They are not an assertion that functions or a live data set already exist.

```text
assert live_receiver.accepts(offline_frame) is False
assert subscriber.send_capture(valid_capture).code == "ROLE_DENIED"
assert after_restart.spool_pending == before_unacked_payloads
```

## Exact verification commands
Commands run from repo root after owning tasks exist. Commands needing real data or user secret run only at the external gate, in the user terminal.

```bash
uv run --frozen --offline python -m pytest -q tests/live/test_live_wire.py tests/live/test_live_spool_bridge.py
pnpm --dir extension exec tsc -p tsconfig.test.json
node --test extension/.test-build/test/live/spool.test.js extension/.test-build/test/live/wire.test.js
```

## Acceptance
- The authenticated local wire writes to the real LiveStore. Wrong secrets, roles or origins produce zero application writes. Part A remains functional and is retested separately.

## Error handling and continuation
Ordinary test/code errors are fixed automatically; then continue the next ready task. A missing key/profile/approval/platform is WAITING_INPUT with one precise user instruction, not a request after every task. Run other ready independent tasks meanwhile. Never treat synthetic data as a substitute for a real-case gate. Authentication failure, scope mismatch, secret leakage or an unsafe privilege change is a stop for the affected real source. Quota/runtime limits never auto-expand.

## Rollback
Stop owned source/session if needed, preserve evidence, and revert only this task’s change through a normal reviewed commit. No hard reset, force push, deletion of other work or mutation of old PASS receipts.
