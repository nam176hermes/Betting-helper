# PB-05 — Normalize football snapshots and preserve correction/missingness lineage

**Legacy mapping:** LV-02/03
**Dependencies:** PB-04
**External input gate:** NONE
**Status:** PLANNED — no implementation claim.

## Files owned/modified
- `src/moj_discovery/providers/football_normalizer.py`
- `tests/live/test_provider_normalization.py`
- `tests/live/test_provider_corrections.py`

## Interfaces to produce
- `football_normalizer.py::normalize_bundle(response: ProviderResponse, selected: Sequence[int], observation: ObservationStamp) -> BundleProjection`
- `football_normalizer.py::compare_event_sets(old: EventSet, new: EventSet) -> EventRevision`

## Read before implementation
A1; A2; A3 in `SOURCES.md`; the relevant specification in02..06. File paths are repo-relative, not paths in this plan package. Future paths are created by their owner before verification, not required at PB-00.

## Test-first execution
- [ ] Write focused positive/rejection tests in the exact test files below. A first red must fail because the intended behavior is wrong/missing, not an unrelated import/configuration error.
- [ ] Map only allowed fields from02_PROVIDER and04_CONTRACTS. kickoff timestamp is not update time; null clock/score remains null. Preserve selected-but-missing IDs as missing observation facts.
- [ ] Create normalized immutable event multiset with deterministic ordering and duplicate multiplicity. Retain batch receipt separately from semantic content revision.
- [ ] Change/removal of goal/card creates correction revision; never incrementally accumulate every poll. Unknown man-state and event detail stay unknown rather than guessing.
- [ ] Drop unneeded players/stats/lineups bodies; record availability only. No extra endpoints to fetch dropped data. Redacted persisted output strict schema validates.
- [ ] Run all owned tests and affected regression checks. Inspect actual outputs and diff; preserve failed evidence. Fix code rather than change expected values to get green.
- [ ] Checkpoint task ID, code/config hashes, command exits and actual evidence in `.local/part-b/task-evidence/PB-05.json`; update `docs/execution/PART_B_PROGRESS.md`. Commit only task files under explicit local-commit authority; never push.

## Behavioral assertions to implement in owned tests
These are specification assertions; helper fixtures belong to this task’s test files. They are not an assertion that functions or a live data set already exist.

```text
assert normalize(response_with_null_goals).score_current is None
assert event_hash([event_a, event_b]) == event_hash([event_b, event_a])
assert normalize(response).provider_updated_at is None  # kickoff is not update time
```

## Exact verification commands
Commands run from repo root after owning tasks exist. Commands needing real data or user secret run only at the external gate, in the user terminal.

```bash
uv run --frozen --offline python -m pytest -q tests/live/test_provider_normalization.py tests/live/test_provider_corrections.py
```

## Acceptance
- Reordering an unchanged event multiset preserves its semantic hash. Null differs from zero. An omitted fixture cannot corrupt another fixture. Score/period conflicts remain explicit.

## Error handling and continuation
Ordinary test/code errors are fixed automatically; then continue the next ready task. A missing key/profile/approval/platform is WAITING_INPUT with one precise user instruction, not a request after every task. Run other ready independent tasks meanwhile. Never treat synthetic data as a substitute for a real-case gate. Authentication failure, scope mismatch, secret leakage or an unsafe privilege change is a stop for the affected real source. Quota/runtime limits never auto-expand.

## Rollback
Stop owned source/session if needed, preserve evidence, and revert only this task’s change through a normal reviewed commit. No hard reset, force push, deletion of other work or mutation of old PASS receipts.
