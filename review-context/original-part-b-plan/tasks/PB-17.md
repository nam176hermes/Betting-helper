# PB-17 — Run the complete mock Part B gate and prepare the exact external-input checklist

**Legacy mapping:** all LV code boundary
**Dependencies:** PB-06, PB-07, PB-08, PB-09, PB-10, PB-11, PB-12, PB-13, PB-14, PB-15, PB-16
**External input gate:** NONE
**Status:** PLANNED — no implementation claim.

## Files owned/modified
- `tests/live/test_part_b_inventory.py`
- `.github/workflows/part-b.yml`
- `docs/live/mock-handoff.md`
- `docs/execution/PART_B_PROGRESS.md`

## Interfaces to produce
- `tests/live/test_part_b_inventory.py::test_every_required_case_has_executed_evidence`

## Read before implementation
R5; A1; C2 in `SOURCES.md`; the relevant specification in02..06. File paths are repo-relative, not paths in this plan package. Future paths are created by their owner before verification, not required at PB-00.

## Test-first execution
- [ ] Write focused positive/rejection tests in the exact test files below. A first red must fail because the intended behavior is wrong/missing, not an unrelated import/configuration error.
- [ ] Execute every required mock case in the acceptance registry with explicit Python and compiled TypeScript paths. Real-browser-required cases must use the recorded platform; collection or skipped cases do not count as PASS.
- [ ] Add portable mock CI on push/PR and an opt-in browser job. Neither job needs real API credentials. Do not push or remote-dispatch the workflow without permission.
- [ ] Run affected Part A spool/canonical/transport regressions and final acceptance when shared sources changed. Keep legacy full qualification separately reported.
- [ ] Inspect the diff and output current source-bound mock evidence and the external-input checklist. Proceed to PB-18 only with honest gate results.
- [ ] Run all owned tests and affected regression checks. Inspect actual outputs and diff; preserve failed evidence. Fix code rather than change expected values to get green.
- [ ] Checkpoint task ID, code/config hashes, command exits and actual evidence in `.local/part-b/task-evidence/PB-17.json`; update `docs/execution/PART_B_PROGRESS.md`. Commit only task files under explicit local-commit authority; never push.

## Behavioral assertions to implement in owned tests
These are specification assertions; helper fixtures belong to this task’s test files. They are not an assertion that functions or a live data set already exist.

```text
assert set(report.executed_case_ids) == set(required_mock_ids)
assert report.live_read_only_pass is False
assert report.money_ready is False
```

## Exact verification commands
Commands run from repo root after owning tasks exist. Commands needing real data or user secret run only at the external gate, in the user terminal.

```bash
uv run --frozen --offline python tools/verify_part_b.py --profile mock --output .local/part-b/mock
```

## Acceptance
- The complete mock gate is verified, or concrete blockers are reported. Codex asks for the API key only because an actual provider probe is now the next gated action.

## Error handling and continuation
Ordinary test/code errors are fixed automatically; then continue the next ready task. A missing key/profile/approval/platform is WAITING_INPUT with one precise user instruction, not a request after every task. Run other ready independent tasks meanwhile. Never treat synthetic data as a substitute for a real-case gate. Authentication failure, scope mismatch, secret leakage or an unsafe privilege change is a stop for the affected real source. Quota/runtime limits never auto-expand.

## Rollback
Stop owned source/session if needed, preserve evidence, and revert only this task’s change through a normal reviewed commit. No hard reset, force push, deletion of other work or mutation of old PASS receipts.
