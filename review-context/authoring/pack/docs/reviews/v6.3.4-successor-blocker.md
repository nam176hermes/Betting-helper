# v6.3.4 successor blocker

v6.3.4 passed its package integrity checks but cannot be approved for implementation. The ownership registry requires `runtime/review-config/review-authority.v1.json` to be generated by MIG0-T05, while the sealed MIG0-T05 task card and command-I/O record omit that artifact. A local generator correction exposed the mismatch; it cannot amend the immutable v6.3.4 contract.

This v6.3.6 successor preserves the v6.3.4 ZIP, sidecar, external attestation and defect evidence under `docs/sources/`, pins their hashes in `source-inputs.v1.json`, and replaces the binding contract. No v6.3.4 approval, task evidence or review result is carried forward.
