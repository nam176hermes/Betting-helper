# Đánh giá khả năng dùng plan v6.3.3 để code

**Kết luận: CHƯA SẴN SÀNG giao Codex triển khai trọn luồng.** Sáu blocker dưới đây buộc người triển khai phải sửa contract hoặc tự chọn cách vượt qua mâu thuẫn. Đây là review kỹ thuật chỉ đọc, không phải receipt Review A/B được ký ở P10. Không sửa pack đã seal hoặc runtime.

Target ZIP SHA-256: `abc8c12f15313d75edb0ed8f7a16c99501997b7a0c797119b8278eff0339bbed`.

## Finding được xác nhận

Mọi đường dẫn docs/ bên dưới tương đối với `hybrid-discovery-v6.3.3-authoritative-design-plan/`.

### R633-01 — P1: MIG0 không được quyền sửa chính 28 file cần rebind

`docs/registries/version-rebinding.v1.json.patches` khai báo 28 phép sửa cho MIG0-T03. Đối chiếu inventory cho thấy 28/28 file không có MIG0-T03 trong `modifying_tasks`. Ví dụ `docs/registries/artifact-ownership.v1.json:7702` khai báo `runtime/tests/bootstrap/test_repository_baseline.py` được tạo bởi MIG0-T02 và `modifying_tasks: []`, nhưng `version-rebinding.v1.json:304` yêu cầu thay nội dung file ở MIG0-T03. Contract `08-execution-and-ownership.md:5` bắt buộc tuân thủ owner/modifier.

Hệ quả: MIG0-T03 không có đường thực thi hợp lệ theo chính inventory. Sửa tối thiểu: cấp MIG0-T03 quyền modifier cho đúng patch targets; checker phải đối chiếu từng patch với quyền này và có negative vector bỏ một modifier.

### R633-02 — P1: Rebinding giữ lại đường dẫn inherited không tồn tại

`docs/registries/version-rebinding.v1.json:304` đổi PACK_ROOT nhưng giữ suffix cũ. Source nền `discovery-runtime/tests/bootstrap/test_repository_baseline.py:113` đọc `pack/docs/schemas/meta-records.schema.json`; dòng435 đọc `pack/docs/vectors/repo0-baseline-roots-v1.json`. Cả hai không có trong seed registry hoặc artifact inventory. File được giao nằm dưới `pack/docs/inherited/v6.2/`.

Đã tái hiện bằng exact seed registry trong TemporaryDirectory và function gốc: `FileNotFoundError` ở lần đọc schema đầu tiên. Không chạy toàn bộ runtime suite. Sửa tối thiểu: rebind đầy đủ schema/vector operands và đối chiếu toàn bộ consumer kế thừa với normative map; chỉ thêm alias nếu được chọn rõ trong contract và có owner.

### R633-03 — P1: Crash test dùng JS được build trước khi implementation thay đổi

`docs/registries/artifact-ownership.v1.json:5439` cho compiled IndexedDB child được tạo bởi MIG0-T07, chỉ có modifier P05-T09, nhưng consumer là P03-T03. Trong cùng file dòng6223, TypeScript source lại được triển khai tại P03-T02. Task P03-T02 chỉ đăng ký TEST_V633_P03_T02, không có compiler operation/output ownership cập nhật child JS.

Hệ quả: P03-T03 dùng output cũ hoặc phải tự thêm rebuild trái inventory. Sửa tối thiểu: đăng ký compile và modifier output sau khi P03-T02 sửa source, trước khi P03-T03 dùng nó; kiểm tra source/output freshness ngay tại consumer.

### R633-04 — P1: Exact argv của security test không chạy trên Node đã pin

`docs/registries/task-command-registry.v1.json:2522` đăng ký `["node", "--test", "extension/.test-build/test/security"]`. Trên Node `v22.23.0`, tái hiện trong thư mục tạm có một test hợp lệ bên trong path đó vẫn exit1, `MODULE_NOT_FOUND`: directory operand không trở thành danh sách test files. Cùng command được dùng ở P05-T09 và candidate/baseline replay.

Sửa tối thiểu: argv liệt kê đúng các compiled test files đã sở hữu, hoặc entrypoint runner được định nghĩa và sở hữu rõ; xác nhận không có suite bị bỏ qua. Đồng bộ registry replay.

### R633-05 — P1: Schema không chấp nhận authorization của Review A/B

`docs/schemas/review-launch-authorization.schema.json:122` và `:143` còn regex đường dẫn v6.3.2. Cấu hình `docs/configs/review-a.v1.json` và `review-b.v1.json` yêu cầu v6.3.3. Dùng Draft202012Validator trực tiếp trên property schemas: cả bốn workspace/output values của A/B đều bị từ chối.

Hệ quả: P10 không thể tạo authorization vừa đúng schema vừa đúng config. Sửa tối thiểu: sửa version trong pattern; thêm positive instances từ hai cấu hình thật và negatives sai root/cross-role.

### R633-06 — P1: Review A thiếu bằng chứng SEALED trong namespace được cấp

`docs/registries/review-command-registry.v1.json:188` yêu cầu A_CHECK_EVIDENCE chạy stage SEALED với evidence root trong final pack. `proof-coverage-matrix.v1.json:18` map seal row vào `evidence/V633-P09-T02.json`. Nhưng `docs/contracts/04-verification-coverage.md:5` chỉ copy CANDIDATE evidence, còn SEALED phải lấy từ external attestation.

`docs/configs/review-a.v1.json:19` chỉ mount final pack và final runtime; không mount sibling attestation. Authorization schema dòng127 giới hạn đúng hai mounts. Command-I/O cũng không khai báo attestation input. Hệ quả: reviewer tuân thủ isolation không thể đọc bằng chứng bắt buộc; không được giải quyết bằng cách nhét final ZIP identity vào pack gây self-reference.

Sửa tối thiểu: thống nhất SEALED evidence source và binding vào external attestation; khai báo exact readonly input, signed authorization binding, command argv và owner/consumer tương ứng.

## Bằng chứng PASS và giới hạn

- `python3 -B review-packs/hybrid-discovery-v6.3.3-authoritative-design-plan/plan_tools/seal_plan.py --root review-packs/hybrid-discovery-v6.3.3-authoritative-design-plan --output-dir review-packs --check`: PASS; 148 manifest entries, ZIP rebuild byte-identical, attestation binding PASS.
- `python3 -B -m unittest discover -s review-packs/hybrid-discovery-v6.3.3-authoritative-design-plan/plan_tests -v`: 10/10 PASS.
- 62 tasks, 194 commands, 852 artifacts; 1.238 schema references resolve qua JSON Pointer.
- 46 crash case IDs, 65 named clock vectors và 16 mapping negatives có coverage tĩnh. Chưa thực thi process crash/clock qualification tương lai.
- Lifecycle gate order và proof stage order đã phân biệt; chưa bảo đảm build freshness như R633-03.
- A/B có root riêng, trust/freshness/aggregation rules được mô tả; R633-05/06 vẫn chặn execution.
- Các negative tests hiện có bắt một số production/placeholder/dependency/rollback/count/owner lỗi. Chúng không bao phủ sáu cross-contract failures trên. Vì vậy PASS của self-check không chứng minh fresh-Codex executability.
- Runtime nền vẫn clean; không discovery, provider call, install, production, signing, hoặc real review launch.

## Verdict

PLAN_CONTRACT_COMPLETE: NO
TASKS_EXECUTABLE_BY_FRESH_CODEX: NO
FINDING_COVERAGE_COMPLETE: NO
SECURITY_REVIEW_CONTRACT_COMPLETE: NO
AUTHORIZED_PRODUCTION_PHASES: NONE

Chỉ nên dùng làm tài liệu thiết kế lúc này. Trước khi giao code trọn luồng: sửa sáu blocker trong successor chưa seal, bổ sung regression checks cho từng lỗi, tái seal và review lại. Không cần thiết kế lại toàn bộ plan.

V633_PLAN_REJECTED
