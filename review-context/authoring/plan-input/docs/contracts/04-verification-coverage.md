# Proof coverage

proof-coverage-matrix.v1.json binds each control to an owner, implementation/test, exact vector IDs, command and evidence stage. command-results bind actual argv/cwd/environment hashes, exit status, output hashes and the current source/lock/build roots. Expected fields alone cannot pass a check. Missing, skipped, stale, duplicate or declaration-only evidence is a failure for eligible rows.

Runtime task evidence lives only under /home/thenam176/betting-helper/authoring-evidence/hybrid-discovery-v6.3.6. P09 copies only the explicitly mapped CANDIDATE evidence into final pack/evidence before governed hashing. The SEALED matrix row consumes only the external runtime seal attestation at the exact --attestation operand, with sealed_evidence_path=null and owner P09-T04. There is no evidence/V636-P09-T02.json delivery. SEALED-stage and REVIEWED-stage completion receipts remain external; they cannot become input to their own seal. At final review, the sealed-evidence mapping resolves CANDIDATE rows to final pack/evidence and seal-stage hashes to the external attestation. P10 review output remains outside pack and baseline.

All 46 inherited crash cases are mandatory exactly once in the registry and once per parent matrix execution. Child-command IDs are explicit launch recipes; candidate replay does not independently execute them again. inspect_restart_state reads restarted real test stores, never the expected record. Compare every expected field, observe the named checkpoint, terminate the child as specified, restart and inspect, and run input/expected-state mutations. Child exit codes reflect termination; only the supervising matrix returns zero on correct evidence. Chrome uses a disposable test profile, never an authenticated profile. Offline loopback exists only inside the isolated test network; external network is denied.

All 65 named clock vectors, including 16 mapping negatives, are mandatory in Python/TypeScript. Frozen precedence uses the ordered list, first applicable failing predicate wins. Arithmetic/ownership/realm inputs are recomputed. Claimed derived interval/RTT/uncertainty fields are either independently verified against raw inputs or tested as separate validation-boundary inputs; they never override recomputation. Unknown source age is not zero age. Mutation-sensitive evaluation and cross-language parity are required.

SEALED verification requires --attestation, --zip and --sidecar. Recompute archive hash, exact sidecar text, archive-to-directory file closure, manifest hash, governed root, task-manifest/self-review hashes and REPO0/candidate bindings; validate the RUNTIME_PACK attestation schema. The external attestation hash is signed as a file mount in the host launch record. A missing input or inconsistent binding fails, never silently falls back to authoring evidence. CANDIDATE verification requires none of these future artifacts.
